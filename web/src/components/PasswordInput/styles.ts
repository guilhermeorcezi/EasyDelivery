import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  position: relative;

  .eye-icon {
    position: absolute;
    cursor: pointer;
    top: 5;
    right: 42px;
  }
`;
